<%# NOTE: this form uses bootstrap_form_for and bootstrap_forms helpers - see: https://github.com/sethvargo/bootstrap_forms %>

<%= f.text_field :title, label: 'Thesis title', required: true, help_block: 'Please limit titles to 250 characters or less.' %>

<% contributor = @contribution.creator %>
<% if @contribution.creator.nil? %>
   <% contributor = current_user.display_name  %>
<% end %>
<%= f.text_field :creator, label: 'Contributor', value: contributor, required: true, readonly: 'readonly'  %>

<%= f.text_field :department, label: 'Department', 'data-autocomplete': '/authorities/search/local/departments'  %>

<script>
  Tufts.autocomplete()
</script>

<%= f.select :embargo_note, standard_embargos,  label: 'Embargo'  %>

<span class="help-block">An embargo will restrict all access to your work until the selected time has passed.</span>

<%= f.text_area :description, label: 'Short Description', help_block: 'Please limit description to 2000 characters or less.', rows: 5, class: 'col-sm-8', required: true  %>
