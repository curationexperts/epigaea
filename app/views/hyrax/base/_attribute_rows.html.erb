<% Tufts::Terms.all_terms.each do |term| %>
  <% presenter_value = presenter.send(term) %>
  <% unless presenter_value.nil? || presenter_value.empty? || presenter_value == [''] %>
  <%= presenter.attribute_to_html(term) %>
  <% end %>
<% end %>

